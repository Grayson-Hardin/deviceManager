# Roadmap


1. Create Persons table in DB
2. JOIN the tables together
3. Create an Add Person feature which adds an entry 
4. On the Edit page, instead of a first / last name, change it to a drop down 
5. Drop down should allow editor to select a different person and input fields should update
